<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="adminSign">

	<!-- 목록조회 -->
 	<select id="getAdminSignList" resultType="adminSignVo">
		SELECT 
			NO
			  , TITLE
			  , CONTENT
			  , ENROLL_DATE
			  , STATUS
			  , DELYN
			  , MODIFY_DATE
		FROM ADMINSIGN
		WHERE DELYN = 'N'
		ORDER BY NO DESC
	</select>
	
	<!-- 목록조회 페이징-->
	<select id="getAdminSignListCnt" resultType="int">
		SELECT COUNT(*)
		FROM ADMINSIGN
		WHERE DELYN = 'N'
	</select>
	
	<!-- 기안문 가져와서 넣기 -->
	<insert id="insertList">
		INSERT INTO ADMINSIGN
		(
			NO
			, TITLE
			, CONTENT
			, ENROLL_DATE
		)
		VALUES
		(
			SEQ_ADMINSIGN_NO.NEXTVAL
			, #{title}
			, #{content}
			, #{enrollDate}
		)
	</insert>
	
	<!-- 사용자가 기안한 문서 리스트 조회 -->
	<select id="selectSignList" resultType="adminSignVo">
		SELECT
			NO
			, TITLE
			, CONTENT
			, ENROLL_DATE
			, STATUS
		FROM ADMINSIGN
		WHERE DELYN = 'N'
		ORDER BY NO DESC
	</select>
	
	<!-- 사용자가 기안한 문서 리스트 VO에 있는거 가져온거??? -->
	<select id="getMySignList" resultType="mySignVo">
		SELECT *
		FROM MYSIGN
		WHERE DELYN = 'N'
		ORDER BY NO DESC
	</select>
	
	<!-- 사용자가 기안한 문서 자세히보기 -->
	<select id="selectSignDetail" resultType="mySignVo">
		SELECT
			   NO
			 , SIGN_TITLE
			 , CONTENT
		  FROM MYSIGN
		 WHERE NO = #{num}
	</select>
	
	<select id="getAdminSign" resultType="adminSignVo">
		SELECT * FROM ADMINSIGN
		WHERE NO = #{num}
		ORDER BY NO DESC
	</select>
	
	<!-- 반려처리하기 -->
	<update id="disapproval">
		UPDATE ADMINSIGN
			SET
				STATUS = 'X'
		WHERE NO = #{no}		
	</update>
	
	
</mapper>










