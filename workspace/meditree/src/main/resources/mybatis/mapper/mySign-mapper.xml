<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mySign">

	<!-- 목록조회 -->
	<select id="getMySignList" resultType="mySignVo">
		SELECT 
			NO
			, SIGN_TITLE
			, CONTENT
			, ENROLL_DATE
			, ENROLL_DATE
			, STATUS
			, DELYN
			, MODIFY_DATE
		FROM MYSIGN
		WHERE DELYN = 'N'
		ORDER BY NO DESC
	</select>
	
	<!-- 목록조회 페이징-->
	<select id="getMySignListCnt" resultType="int">
		SELECT COUNT(*)
		FROM MYSIGN
		WHERE DELYN = 'N'
	</select>
	
	<!-- 기안하기 -->
	<insert id="write">
		INSERT INTO MYSIGN
		(
		     NO
			  , SIGN_TITLE
			  , CONTENT
	    )
		VALUES
		(
	        SEQ_MYSIGN_NO.NEXTVAL
	          , #{signTitle}
	          , #{content} 
		)
	</insert>
	
		<insert id="insertToAdmin">
		INSERT INTO ADMINSIGN
		(
			NO
			, TITLE
			, CONTENT
			, ENROLL_DATE
		)
		VALUES
		(
			SEQ_ADMINSIGN_NO.NEXTVAL
			, #{signTitle}
			, #{content}
			, #{enrollDate}
		)
	</insert>
	
	<!-- 사용자 결재양식선택 리스트 조회 -->
	<select id="selectFormList" resultType="mySignVo">
		SELECT
			   NO
			 , SIGN_TITLE
			 , CONTENT
		  FROM MYSIGN
		 WHERE DELYN = 'N'
		   AND STATUS = 'O'
		 ORDER BY NO DESC
	</select> 
	
 	<!-- 사용자 결재양식선택 리스트 상세조회 -->
	<select id="selectFormDetail" resultType="mySignVo">
		SELECT
			   NO
			 , FORM_TITLE
			 , CONTENT
		  FROM APPR_FORM
		 WHERE NO = #{num}
	</select> 
	
	<select id="getApprList" resultType="apprVo">
		SELECT *
		FROM APPR_FORM
		WHERE DELYN = 'N'
		ORDER BY NO DESC
	</select>
	
	<!-- 기안한거 상세보기 -->
	<select id="getMySign" resultType="mySignVo">
		SELECT 
	      NO
	       , SIGN_TITLE
	       , CONTENT
	       , ENROLL_DATE
	       , STATUS
	       , DELYN
		FROM MYSIGN
		WHERE NO = ${num}
		AND   DELYN = 'N'
	</select>
	
	<!-- 기안문 수정하기 -->
	<update id="edit">
		UPDATE MYSIGN
		SET
			SIGN_TITLE = #{signTitle}
			, CONTENT = #{content}
		WHERE NO = #{no}
	</update>
	
	<!-- 기안문 승인처리되면 상태 바뀌게 하는 update -->

	
</mapper>
