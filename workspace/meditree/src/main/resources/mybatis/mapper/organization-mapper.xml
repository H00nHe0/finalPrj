<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="organization">

  
  <select id="selectSurgery" resultType="memberVo">
	SELECT
	    E.NO,
	    E.NAME,
	    E.ENROLL_DATE,
	    E.TEL,
	    E.EMAIL,
	    E.ADDRESS,
	    E.BIRTH,
	    D.MAJOR,
	    D.TITLE
	FROM
	    DEPARTMENT D
	    JOIN EMPLOYEE E ON D.NO = E.DEPT_NO
	WHERE
	    E.QUIT_YN = 'N'
	    AND D.MAJOR = '${major}'
  </select>
  
  <select id="generalMedicine" resultType="memberVo">
	SELECT
	    E.NO,
	    E.NAME,
	    E.ENROLL_DATE,
	    E.TEL,
	    E.EMAIL,
	    E.ADDRESS,
	    E.BIRTH,
	    D.MAJOR,
	    D.TITLE
	FROM
	    DEPARTMENT D
	    JOIN EMPLOYEE E ON D.NO = E.D_NO
	WHERE
	    E.QUIT_YN = 'N'
	    AND D.MAJOR = '${major}'
  </select>
    <select id="ent" resultType="memberVo">
	SELECT
	    E.NO,
	    E.NAME,
	    E.ENROLL_DATE,
	    E.TEL,
	    E.EMAIL,
	    E.ADDRESS,
	    E.BIRTH,
	    D.MAJOR,
	    D.TITLE
	FROM
	    DEPARTMENT D
	    JOIN EMPLOYEE E ON D.NO = E.D_NO
	WHERE
	    E.QUIT_YN = 'N'
	    AND D.MAJOR = '이비인후과'
  </select>
    <select id="psychiatry" resultType="memberVo">
	SELECT
	    E.NO,
	    E.NAME,
	    E.ENROLL_DATE,
	    E.TEL,
	    E.EMAIL,
	    E.ADDRESS,
	    E.BIRTH,
	    D.MAJOR,
	    D.TITLE
	FROM
	    DEPARTMENT D
	    JOIN EMPLOYEE E ON D.NO = E.D_NO
	WHERE
	    E.QUIT_YN = 'N'
	    AND D.MAJOR = '${major}'
  </select>
  
  
  </mapper>